{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "blowsh": {
      "type": "local",
      "command": ["docker", "run", "--rm", "-i", "blowsh-mcp:latest"],
      "enabled": true,
      "timeout": 30000
    }
  },
  "permission": {
    "blowsh_*": "allow"
  }
}
